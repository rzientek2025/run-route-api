{
  "name": "run-route-api",
  "version": "1.0.6", // Nowa wersja
  "description": "API for calculating and storing running routes using Google Maps.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@googlemaps/google-maps-services-js": "^3.3.16",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
    // Usunięto: "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
